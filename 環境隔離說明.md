# 環境隔離設計說明

## 問題分析

您的觀察非常正確！原本的配置存在**環境污染**問題：

### 原始問題
```
系統環境變數 NODE_ENV=production
    ↓
影響 npm run dev ❌  (開發模式被迫使用生產配置)
    ↓
影響 Gradle build ✓  (這是預期的)
```

### 核心矛盾
- **Gradle 建置** 需要 `NODE_ENV=production` 來生成正確的靜態文件
- **npm dev** 需要 `NODE_ENV=development` 或不設定
- 兩者**共用同一個環境變數**，導致互相干擾

## ✅ 正確的解決方案：環境隔離

### 設計原則

**1. Gradle 建置環境（生產）**
- 在 `build.gradle` 中**明確設定** `NODE_ENV=production`
- 不依賴系統環境變數
- 確保建置結果一致

**2. npm dev 環境（開發）**
- **不讀取**系統環境變數的 NODE_ENV
- 使用 Next.js 預設的開發模式
- 完全獨立於 Gradle

### 實現方式

#### 1. build.gradle 配置

```gradle
tasks.register('buildNextjs', Exec) {
    dependsOn 'installNextjs'
    workingDir file('../nextjs-dynamic-api-manager').absolutePath
    def nodeDir = file('../tools/node-v22.20.0-win-x64').absolutePath
    def npmPath = "${nodeDir}/npm.cmd"

    // ✅ 明確設定 NODE_ENV 為 production
    // 這個設定只在 Gradle 執行時有效，不影響手動執行 npm
    environment 'NODE_ENV', 'production'
    environment 'PATH', nodeDir + File.pathSeparator + System.env.PATH

    commandLine npmPath, 'run', 'build'
}
```

**優點**：
- ✅ Gradle 建置時強制使用 production 模式
- ✅ 不依賴系統環境變數
- ✅ 建置結果可預測且一致
- ✅ 不影響開發者手動執行 npm 命令

#### 2. npm scripts 配置

```json
{
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build --turbopack"
  }
}
```

**優點**：
- ✅ `npm run dev` 使用 Next.js 預設的開發模式
- ✅ `npm run build` 會被 Gradle 調用，並強制設定 NODE_ENV=production
- ✅ 兩者完全獨立，互不干擾

#### 3. next.config.js 配置

```javascript
const nextConfig = {
  // 根據 NODE_ENV 自動切換配置
  ...(process.env.NODE_ENV === 'production' && {
    output: 'export',
    basePath: '/dynamic-api',
  }),
};
```

**行為**：
- 當 `NODE_ENV=production` 時（Gradle 建置）→ 使用 basePath 和 export
- 當 `NODE_ENV` 未設定時（npm dev）→ 開發模式，不使用 basePath

## 📊 環境對照表

| 場景 | NODE_ENV | basePath | output | 執行方式 |
|------|----------|----------|--------|----------|
| **開發模式** | undefined (or development) | 無 | 無 | `npm run dev` |
| **Gradle 建置** | production (由 Gradle 設定) | /dynamic-api | export | `gradle buildNextjs` |
| **手動生產建置** | production (手動設定) | /dynamic-api | export | `NODE_ENV=production npm run build` |

## 🎯 使用方式

### 開發模式（獨立運行）

```powershell
# 直接啟動，不需要擔心環境變數
cd nextjs-dynamic-api-manager
npm run dev
```

**特點**：
- ✅ 自動使用開發模式
- ✅ 不使用 basePath
- ✅ 熱更新支持
- ✅ 完全不受 Gradle 影響

### 生產建置（Gradle）

```powershell
cd backend
.\gradlew.bat bootRun
```

**流程**：
```
bootRun
  → processResources
    → buildFrontend
      → buildNextjs
        → environment 'NODE_ENV', 'production'  ✅ Gradle 明確設定
          → npm run build
            → Next.js 檢測到 NODE_ENV=production
              → 使用 basePath: '/dynamic-api'
              → 使用 output: 'export'
```

## 🔍 驗證方式

### 1. 驗證 npm dev 不受系統環境變數影響

```powershell
# 即使系統有設定 NODE_ENV，npm dev 也能正常工作
$env:NODE_ENV = "production"  # 模擬系統環境變數
cd nextjs-dynamic-api-manager
npm run dev  # 應該仍然使用開發模式
```

### 2. 驗證 Gradle 建置使用正確的環境

```powershell
cd backend
.\gradlew.bat buildNextjs

# 檢查生成的 index.html
cat src/main/resources/static/index.html
# 應該包含 /dynamic-api/_next/... 路徑
```

## 🛠️ 技術細節

### Gradle 的 environment 設定作用域

```gradle
tasks.register('buildNextjs', Exec) {
    environment 'NODE_ENV', 'production'  // ✅ 只在這個任務執行時有效
    commandLine npmPath, 'run', 'build'
}
```

**作用域**：
- ✅ 只影響這個 Gradle 任務執行的子進程
- ✅ 不修改系統環境變數
- ✅ 任務結束後自動清除
- ✅ 不影響其他並行的進程

### Next.js 的 NODE_ENV 處理

```javascript
// Next.js 內部邏輯
const isProduction = process.env.NODE_ENV === 'production';

// 如果 NODE_ENV 未設定
// next dev → 自動使用開發模式
// next build → 自動使用生產模式
```

## 📋 最佳實踐總結

### ✅ DO（推薦做法）

1. **Gradle 中明確設定環境變數**
   ```gradle
   environment 'NODE_ENV', 'production'
   ```

2. **npm dev 不依賴環境變數**
   ```powershell
   npm run dev  # 簡單直接
   ```

3. **使用條件配置**
   ```javascript
   ...(process.env.NODE_ENV === 'production' && { basePath: '/dynamic-api' })
   ```

### ❌ DON'T（避免做法）

1. **不要在系統級設定 NODE_ENV**
   ```powershell
   # ❌ 不要這樣做
   [Environment]::SetEnvironmentVariable("NODE_ENV", "production", "User")
   ```

2. **不要讓 Gradle 依賴系統環境變數**
   ```gradle
   // ❌ 不好的做法
   environment 'PATH', System.env.PATH
   // ✓ 沒問題，但要明確設定其他變數
   ```

3. **不要在 npm scripts 中硬編碼 NODE_ENV**
   ```json
   // ❌ 避免
   "dev": "NODE_ENV=development next dev"
   ```

## 🎓 架構優勢

### 1. 環境隔離
```
Gradle 環境 ┐
            ├─ 互不干擾
npm 環境   ┘
```

### 2. 可預測性
- Gradle 建置結果**始終一致**
- npm dev 行為**始終一致**
- 不受系統環境影響

### 3. 維護性
- 開發者不需要記憶複雜的環境設定
- 新成員上手容易
- CI/CD 配置簡單

## 🚀 結論

通過在 Gradle 中**明確設定 NODE_ENV**，我們實現了：

1. ✅ **環境隔離**：npm dev 和 Gradle build 互不干擾
2. ✅ **簡化使用**：開發者只需要 `npm run dev`
3. ✅ **一致性**：Gradle 建置結果可預測
4. ✅ **可維護性**：配置清晰，易於理解

這才是正確的架構設計！🎯

---

**設計原則**: 環境隔離 > 配置簡化 > 行為一致
**核心理念**: 各司其職，互不干擾
